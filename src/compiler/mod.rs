
// mod symbol;
// mod datatype;
// mod compiler;
// mod program;

use crate::parser::ast::Ast;
// use compiler::Compiler;

pub fn compile_ast(ast: &Ast) -> String {
    let mut code = String::new();
    // let mut compiler = Compiler::new(ast, &mut code);
    // program::compile_program(&mut compiler);
    code
}
